#!/usr/bin/env bash
# [MISE] hide=true

set -o errexit
set -o pipefail

if [[ -e poetry.lock ]]; then
  poetry build
elif [[ -e uv.lock ]]; then
  UV_DYNAMIC_VERSIONING_BYPASS=${MISE_LIB_CLEAN_VERSION}
  export UV_DYNAMIC_VERSIONING_BYPASS
  uv build
fi
